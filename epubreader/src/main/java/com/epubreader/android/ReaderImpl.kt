package com.epubreader.android

class ReaderImpl(private val readerConfig: ReaderConfig) : Reader {
    override suspend fun openBook(id: String) {

    }

    override suspend fun deleteBook(id: String) {

    }
}